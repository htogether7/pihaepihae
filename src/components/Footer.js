import React from "react";
import styled from "styled-components";

const Footer = () => {
  return <FooterDiv>제작자 : 김시환</FooterDiv>;
};

const FooterDiv = styled.div`
  /* position: fixed;
  bottom: 0;
  left: 50%; */
  margin-top: auto;
`;

export default Footer;
